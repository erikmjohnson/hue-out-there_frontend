import { styled } from "@material-ui/core";
import ToggleButton from "@material-ui/lab/ToggleButton";

const LightButtonMatUI = styled(ToggleButton)({
  background: 'purple',
  color: 'blue',
  border: 'none',
  margin: 5,
  borderRadius: 10,
});

export default LightButtonMatUI;
